---
title: "Frontend"
description: "Crea la tua istanza di eventn-frontend."
---

<Info>
  **Prerequisite**: Node.js (version 21)
</Info>

**Step 0**: Clona il repository:

```bash
git clone https://github.com/T06-EvenTN/frontend.git
cd frontend/
```

**Step 1**: Installa le dipendenze necessarie:

<CodeGroup>

```bash npm
npm install
```

</CodeGroup>

**Step 2**: Clona il file .env.example e inserisci i file necessari:

```bash
cp .env.example .env
# modifica .env con il tuo editor di testo preferito
```

**Step 3**: Pronti al lancio\! usa il seguente comando per lanciare il backend:

```bash
npm run build
# oppure npm run dev per eseguire test in locale
```

### URL backend

L'unico valore necessario per il funzionamento del frontend è l'indirizzo del server backend. L'indirizzo va specificato senza trailing slashes, per esempio:

```markdown
VITE_BACKEND_URL=http://localhost:8080
```